#!/bin/sh

# try to make the iso smaller by removing stuff we don't need.

apt clean
rm -rf /var/cache/apt/*
rm -rf /var/lib/apt/lists/*

rm -rf /usr/share/doc/*
rm -rf /usr/share/man/*
rm -rf /usr/share/info/*

find /usr/share/locale -mindepth 1 -maxdepth 1 ! -name "en*" -exec rm -rf {} +
